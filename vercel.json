{
  "version": 2,
  "buildCommand": "yarn build",
  "installCommand": "yarn install",
  "framework": null,
  "env": {
    "ADMIN_JS_SKIP_BUNDLE": "true"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/$1" }
  ]
}
